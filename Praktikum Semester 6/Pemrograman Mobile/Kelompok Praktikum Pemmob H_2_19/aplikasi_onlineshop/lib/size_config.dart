double setWidth(double inputWidth, double screenWidth) {
  return (inputWidth / 400) * screenWidth;
}

double setHeight(double inputHeight, double screenHeight) {
  return (inputHeight / 800) * screenHeight;
}
